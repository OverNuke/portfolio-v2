
.claude/

.atl/

# Tool-local state (config + hook cache only) — same class as .atl/
.impeccable/

# Holding pen for swept session junk. The USER empties this, never an agent.
_quarantine/

node_modules/
dist/
dist-ssr/
*.local
.DS_Store
*.tsbuildinfo
vite.config.d.ts
test-results/
playwright-report/
